# MMS — Maintenance Management System · Architecture & Development Plan

*v1.1 · July 2026 · Companion to "Backlog — Epics & User Stories (v0)" · Amended 2026-07-22 after internal design review (see §5)*

**What we're building:** a lightweight, mobile-first factory maintenance app — machine register, spare-parts stock with an honest ledger, work-order queue, self-generating PM schedules, QR-based no-login breakdown reporting, and a planner dashboard. English UI, i18n-ready for Bulgarian. One small VPS, a few €/month. Solo developer, AI-assisted, ~6 weeks.

---

## 1. Architecture

### 1.1 Shape: one boring monolith

One deployable: a single web app + database + in-process background scheduler, in **one Docker container** behind a reverse proxy that terminates TLS. No microservices, no separate API+SPA split, no queue. The Definition of Done (server-persisted state, server-enforced permissions, i18n-ready) is easiest to honor when rules live in exactly one place.

### 1.2 Stack

| Layer | Choice | Rationale |
|---|---|---|
| Framework | **Next.js (App Router) + TypeScript** | One language across stack; server components/actions make server-side enforcement and persistence the default path |
| UI | Tailwind CSS + shadcn/ui | Mobile-first, thumb-friendly components without design overhead |
| Database | **SQLite (WAL mode) + Drizzle ORM**, driver `@libsql/client` | Scale (200 machines / 1,000 parts / handful of users) is far below SQLite's ceiling; one file = trivial correct backups; no DB server to run. Driver swapped from better-sqlite3 → libsql: Node 25 is bleeding-edge and libsql ships N-API prebuilts (installs clean, no native compile), same SQLite file + WAL, same backup story |
| Auth | **Hand-rolled `sessions` table** + persistent cookie | "Survives closing the tab" = persistent cookie; ~50 lines beats a young dependency (Better Auth rejected) |
| Background jobs | node-cron registered in **`instrumentation.ts`** | PM generation + daily digest are two cron entries; restart-safety is guaranteed by the idempotency invariants (§1.4), so a missed or doubled tick costs nothing — guard against dev-mode double-registration |
| Images | `sharp` resize-on-upload via a **route handler** (not server actions — body-size limits); files on a disk volume | No object-storage bill; volume backed up with the DB |
| i18n | next-intl: `en.json` for the full app + **`bg.json` for the public report form only (ships in v0)** | Internal UI stays English (E0-S5); but the no-login form is used by the least English-speaking persona — ~20 strings on plumbing that exists anyway |
| Design direction | Flat/utilitarian, safety-orange + slate, Fira Sans/Fira Code — **see DESIGN.md** | Legible in bad factory light, gloved-thumb targets, status never color-alone |
| Email | Nodemailer → free relay tier (**Brevo**, fallback Resend) | Digest (E6-S3) and password reset (E0-S8) share one relay |
| QR | `qrcode` lib + print-CSS A4 label sheet | No PDF pipeline needed |
| Deploy | Docker (multi-stage) + **Caddy** on a small VPS (**Hetzner CX22**, ~€4/mo) | Caddy = automatic HTTPS in two config lines (E0-S1) |
| Backups | Nightly `sqlite3 .backup` + uploads copy via cron, 14-day retention; optional Litestream later | Restore = copy the file back (E0-S6) |

Rejected alternative, for the record: **Django** (would hand us E0 nearly free) — passed over because the app is interaction-heavy on mobile and one language keeps a solo TS build simpler.

### 1.3 Data model — core tables

`users`, `sessions`, `machines`, `downtime_periods`, `parts`, `machine_parts` (fitment: which parts a machine uses — see §6), `stock_movements`, `work_orders`, `work_order_status_history`, `work_order_parts`, `checklist_items`, `photos` (polymorphic: machine/job/report), `pm_schedules`, `reports`, `settings` (factory name, timezone).

Every mutating table carries `created_by` + timestamps (DoD: actor + timestamp on significant actions).

### 1.4 The three load-bearing invariants (schema-enforced)

1. **Stock is a ledger, not a number.** Append-only `stock_movements` (receive / issue / adjust / reverse; qty, actor, timestamp, reason, optional `work_order_id`). `parts.on_hand` is a cached column updated in the same transaction as the movement insert; never negative. Removing a part line from a job inserts a **reversing movement** — never deletes. "Movements sum exactly to on-hand" (E2-S6) is an assertable property.
2. **Downtime lives in one table.** `downtime_periods` (machine_id, started_at, ended_at, opened_by, closed_by, optional `work_order_id`). Machine status "Down" ≡ "has an open period". Duration always computed from the period; nothing else stores a copy (E1-S4, E3-S8).
3. **PM generation is idempotent by schema.** Unique constraint on `(schedule_id, due_date)` in work orders. Daily job = "active schedules where next_due ≤ today + 7 and no WO exists for that due date" — no-duplicates and catch-up-after-downtime are the *same* code path (E4-S2). Next due = completion date + interval (floating, E4-S3).

### 1.5 Cross-cutting mechanics

- **QR = one URL, auth decides the view.** Labels encode `https://app/m/{code}`. Signed-in → machine page; anonymous → public report form. One route serves E1-S5 and E5-S1.
- **Permissions in one module.** `can(user, action, resource)` called at the top of every server action / route handler. Two roles (Admin, Technician) — a policy file, not a framework.
- **Anonymous form hardening (E5-S1, beyond the backlog's honeypot):** rate limit + honeypot + **photo size cap, EXIF strip on upload, per-machine daily submission quota** — the no-login photo upload is a disk-fill/abuse vector otherwise.
- **UTC everywhere.** All timestamps stored UTC; factory timezone applied only at the edges — display, the Today/This-week buckets (E6-S2), and cron scheduling (02:00 backup, morning digest). Naive TZ handling corrupts exactly those two places silently.
- **First-run setup (E0-S7):** empty `users` table → all routes redirect to `/setup` (create admin, factory name, timezone). Timezone drives the scheduler and Today/This-week buckets (E6-S2).
- **Testing where the money is:** no UI test coverage in v0. Integration tests for the three invariants + role enforcement via direct API (E0-S4) — ~15 tests protecting everything that can silently corrupt.

---

## 2. Development plan — 6 weeks, epic order E0 → E6

**Standing rule: every week ends with a pass on a real phone.** Mobile-first claims die on first contact with a gloved thumb — don't discover that in week 6.

### Week 1 — Walking skeleton (E0 core + slice of E1/E3)
- Scaffold repo (git init), Drizzle schema for core tables, auth + sessions, users page with roles.
- **Deploy on day 2–3**, not at the end: Dockerfile + Caddy + Hetzner VPS + domain + HTTPS (E0-S1).
- Skeleton flow: add a machine → create a work order → Open → In progress → Done.
- Backup cron installed this week (retention starts accumulating immediately).
- **Exit criteria:** real URL shows a machine with a completed job; nightly snapshot exists.

### Week 2 — Machines complete (E1)
- Search/filter (E1-S2), machine page with history sections (E1-S3), Down/Running via downtime periods (E1-S4), QR labels + A4 print sheet (E1-S5), retire (E1-S6).
- Stub the anonymous QR target now ("coming soon" page) so pilot labels never need reprinting.

### Week 3 — Stock (E2) + queue pulled forward
- Parts catalog + search (E2-S1/S2), receive / issue / adjust movements (E2-S3/S4/S5), part page with full ledger (E2-S6), low-stock list + nav badge (E2-S7).
- **Machine parts (fitment) — new v0 scope (D11, §6):** the per-machine spares list — attach/remove parts + a "Parts" section on the machine page (E2-S8/S9). Lands here because it needs the parts catalog; the job-logging tie-in + part-page reverse list (E2-S10) ride with E3-S6 in week 4.
- **Work-order queue with overdue-first + filters (E3-S2) — pulled forward from week 4** (same list-rendering muscle as the parts list; de-loads the plan's riskiest week).
- **CSV paste-import for machines and parts** (~half a day, promoted from E8): ~200 machines and ~1,000 parts must exist before the pilot means anything — manual-form entry is days of factory labor; import makes onboarding an afternoon.
- Ledger invariant tests land this week.

### Week 4 — Work orders complete (E3, rebalanced)
- **E3-S6 (parts used on a job) is the only L story — budget two full days.**
- My Work mobile view (E3-S4), checklists (E3-S5), photos + time spent (E3-S7), reassign/reschedule (E3-S9), breakdown-closes-downtime prompt (E3-S8).
- This is the week most likely to slip; if it does, cut Should-stories (S8) immediately rather than compressing weeks 5–6.

### Week 5 — PM + public reporting (E4 + E5)
- PM schedules (E4-S1), generation cron + idempotency tests (E4-S2), floating next-due (E4-S3), overdue flags (E4-S4), schedule register (E4-S5).
- Public report form (E5-S1) **in English and Bulgarian** (`bg.json`, form strings only), hardened per §1.5: rate limit, honeypot, photo cap, EXIF strip, per-machine quota. Triage queue + badge (E5-S2), operator status view (E5-S3).
- Smaller than it looks — reuses week-4 machinery.

### Week 6 — Dashboard, digest, pilot onboarding (E6 + pilot prep)
- Dashboard with five clickable counts (E6-S1), Today/This-week buckets in factory TZ (E6-S2), daily digest email — silent on clean days (E6-S3).
- **One-time restore drill from a real snapshot** (E0-S6 requires it before pilot).
- Password reset (E0-S8) *or* admin-temp-password fallback — pick one, don't ship with neither.
- **Pilot onboarding checklist (explicit workstream, not an afterthought):**
  1. Import real machine + parts data via CSV (week-3 tool).
  2. Print and mount QR labels on every machine.
  3. 30-minute walkthrough with Maria (dashboard, triage, low-stock) and Tomasz (My Work, parts-on-job, Down/Running) on their own phones.
  4. Confirm the digest email arrives on Maria's phone.
- Slow pass on a real phone through every flow.

**Should-stories** (E0-S8, E1-S6, E3-S8, E4-S5, E5-S3, E6-S3) absorb into their epic's week or defer without breaking anything.

### Watch item (from backlog, E4 risk note)
The daily-digest habit is the v0 hedge for "nobody looks at the app". If the planner's login habit doesn't form in pilot week 1, per-event notifications (assignment alerts, Telegram) jump to the top of v0.1 (E7).

---

## 3. Decision log

| # | Decision | Status |
|---|---|---|
| D1 | Stack: Next.js + TypeScript (over Django) | **Default — confirm** |
| D2 | DB: SQLite + Drizzle (over Postgres) | **Default — confirm** |
| D3 | Host: Hetzner CX22 (~€4/mo) | **Default — confirm** |
| D4 | Mail relay: Brevo free tier | **Default — confirm** |
| D5 | Domain name for the app | **Open — BLOCKS week-1 deploy; decide this week** |
| D6 | Password reset path: email self-serve vs admin temp password | Open — decide by week 6 |
| D7 | Switch SQLite → Postgres | Only if multi-tenant SaaS (E8) becomes real |
| D8 | Auth: hand-rolled sessions table (Better Auth rejected — dependency churn) | **Decided 2026-07-22 (design review)** |
| D9 | Public report form ships bilingual EN+BG in v0; internal UI stays English-only | **Decided 2026-07-22 (design review)** |
| D10 | CSV paste-import for machines/parts promoted from E8 into v0 week 3 | **Decided 2026-07-22 (design review)** |
| D11 | Machine parts (fitment / BOM) added to v0 — per-machine spares list; informational, decoupled from the stock ledger; stories + schema in §6 | **Decided 2026-07-23 (user request)** |

---

## 4. Current status

- **2026-07-22 (planning):** Plan written; internal design review (§5) folded in as v1.1. PRODUCT.md + DESIGN.md + SCREENS.md written; dashboard brief approved.
- **2026-07-22 (Week-1 walking skeleton — BUILT + verified):** D1–D4 confirmed ("go"). Stack live: Next.js 16 · React 19 · Tailwind v4 · Drizzle + `@libsql/client`. Full schema (15 tables, all three invariants). Auth: hand-rolled sessions, RBAC, first-run setup, login/logout, proxy (middleware) gate. UI: app shell (sidebar + mobile tabs, IBM Plex, light-mode theme), dashboard (Instrument Panel with green-flip + overdue-first queue), machines (list/new/detail + Down↔Running downtime ledger), work orders (queue/new/detail + Open→In progress→Done, activity log), parts stub. `next build` passes; **end-to-end flow verified in-browser** (setup → machine → downtime open/close → WO created → Done, all actor+timestamped). Deploy artifacts written: Dockerfile, docker-compose, Caddyfile, backup.sh, DEPLOY.md. **Committed to git** (`f6ba655`, initial commit, 191 files). **Not yet done:** actual VPS deploy (blocked on **D5 domain**), restore drill on real snapshot.
- **2026-07-23 (Week-2 Machines / E1 — BUILT + verified + reviewed):** Epic E1 complete. Search/filter (E1-S2) via a reusable URL-driven `SearchFilterBar` (search + status chips + location select, state in URL, status rail, live Down readout, retired hidden by default); edit machine (E1-S1); fuller detail page (E1-S3) with inline-SVG **QR-as-motif**, Completed-work / Preventive-maintenance / Downtime-history sections + honest empty states; retire↔unretire (E1-S6) — retire closes any open downtime period + pauses PM schedules + hides from default lists (invariant #2 preserved; **verified in DB**), unretire restores without reopening; QR label **A4 print sheet** (E1-S5) at `/print/labels` (`?ids=` selection or all-active); and the public **`/m/{code}` QR target** (proxy `PUBLIC_PREFIXES` += `/m/`) — signed-in→machine page, anon+active→"fault reporting coming soon" stub, missing/retired→friendly dead-link, no data leak — so pilot labels never need reprinting. New dep: **`qrcode`** (pure-JS, N-API-free; server-rendered SVG via `lib/qr.ts`; base URL via `lib/url.ts` from `APP_URL` env or request headers). `next build` clean (16 routes); **verified end-to-end in browser**. Hardened via a **5-dimension adversarial review workflow** (Next/RSC + security/RBAC/leak dimensions came back clean); **12 confirmed findings fixed**: touch targets → 44px (filter chips/select/clear) & 56px form primary, `slate-400`→`slate-500` contrast, `markDown`/`markRunning` `retiredAt` guard (defense-in-depth for the retire TOCTOU), LIKE-metacharacter escaping in search, `/m/[code]` double-decode removed (was 500 on `%` codes), retired machines excluded from label printing, empty/garbage `?ids=` no longer prints the whole fleet, single orange primary on empty machine list, garbage `?status=` falls back to "All". **Not yet committed** (awaiting user); VPS deploy still blocked on **D5 domain**.
- **2026-07-23 (Week-3 Stock / E2 core — BUILT + verified + reviewed):** E2 stock foundation done. `lib/stock.ts` `recordMovement` — the invariant-#1 primitive (signed movement + cached `on_hand` in one `db.transaction`, never negative; zero-delta is a no-op). Parts catalog + search + low-stock filter (`searchParts`), new/edit part, part detail with the **movement ledger** (200-row cap, truncation signalled), receive/issue/adjust via a shared native-`<dialog>` (`stock-dialog.tsx`, form remounts per open), low-stock **nav badge** (layout `countLowStock`). `next build` clean (19 routes). **Verified end-to-end in browser + DB:** receive/issue/adjust, insufficient-stock refused & rolled back, opening-stock-on-create, and **`SUM(movements) == on_hand` asserted for every part**. Hardened via a ledger-focused adversarial review workflow — **invariant-correctness + security/RBAC came back clean** (a verifier confirmed drizzle `db.transaction` runs `BEGIN IMMEDIATE`, so no lost-update under concurrency); **10 confirmed UX/craft findings fixed** (dialog stayed open on a repeat submit → keyed remount; stale error; dialog a11y name + 44px close/cancel + backdrop-dismiss; mobile badge contrast; slate-400→slate-500 figures; amber "need N"; ledger truncation note; zero-delta adjust no-op). **Not committed.**
- **2026-07-23 (Machine parts / E2-S8/S9/S10 — BUILT + verified + reviewed):** the machine↔part fitment feature (§6) — `machine_parts` table + migration, attach/remove, machine Parts section, part Fits-machines list. Decoupled from the stock ledger (verified). Review clean on correctness/security/RSC; 4 low/nit fixes applied. `next build` clean (20 routes). **Not committed.**
- **2026-07-23 (E2 completed to AC + Jira synced):** filled the last E2 acceptance-criteria gaps so all 7 stories meet their ACs — **part photos** (client-canvas compress → auth-gated route handler `/parts/[id]/photo` GET/POST/DELETE → `./uploads/parts/`; no `sharp`, Node-25-safe; reusable for E3-S7 job photos), **SKU auto-suggest** (`P-###`), and the **adjust ledger now shows before→after** (`8 → 6`). `next build` clean (20 routes). **Jira board synced** (`empyriasoft.atlassian.net`, project MMS): E0 login/roles/setup + all E1 + all E2 (10 stories, incl. 3 new fitment stories MMS-51/52/53) + E3 create/lifecycle → **Done**; E1/E2 epics → **Done**; E0/E3 epics + E3-S2 → **In Progress**. E2 (MMS-3) is fully Done. (Machine photos — the E1-S1 photo AC — share this same pipeline and are the one trivial follow-up not yet built.)
- **2026-07-23 (Week-3 queue / E3-S2 — BUILT + verified + reviewed):** The work-order queue now sorts **overdue-first** and filters by status / assignee / machine / priority (E3-S2). New `searchWorkOrders` + faceted `workOrderStatusCounts` + `queueFilterOptions` (queries.ts); **status tabs with live counts** (`queue-tabs.tsx` — server-rendered `<nav>` links, non-status filters carried forward, all state in the URL); queue page rewritten around the reusable `SearchFilterBar` (title search + assignee/machine/priority selects) with the overdue-first list (day-granular overdue → red rail, **Due today** amber, closed statuses most-recent-first, 100-row cap + truncation note). **Verified in browser + DB:** overdue ordering (12d→5d→2d→1d→Due today→future→undated), faceted counts (Active/Open/In-progress/Done/Cancelled), the Tomasz+High combined filter, URL-survives-refresh, mobile 375px (no page overflow, tab bar scrolls). `next build` clean (20 routes). Hardened via the **adversarial review workflow** (correctness / security-RBAC / UX-craft → independent verify) — security-RBAC came back clean; **6 confirmed findings fixed:** faceted-count empty states (was "No work orders yet" when everything was closed), nav semantics (dropped `role=tablist/tab` → `<nav>` + `aria-current`), mobile chip stacking (title column ~40px → 130px), day-granular overdue + "Due today" (killed the "0d over" papercut), stale/garbage filter-param fallback in the shared `SearchFilterBar`, and 8px tab gap. **MMS-32 → Done.** **Not committed.**
- **2026-07-23 (Week-4 pulled forward / E3-S6 — BUILT + verified + reviewed):** **Record parts used on a job** (the epic's L-story) — exercises invariant #1 in the work-order context and completes the **E2-S10 quick-pick**. `lib/stock.ts` refactored so `recordMovement` shares a tx-aware core (`recordMovementTx`); new **`issuePartToWorkOrder`** and **`reverseWorkOrderPart`** write the stock movement AND the `work_order_parts` line in **one transaction** (a line can never exist without its ledger movement), with the job-status gate re-checked *inside* the write-locked tx and a double-reverse guard. New `listWorkOrderParts` (non-reversed lines + per-line cost); `addPartToJob`/`removePartFromJob` actions (`work:log-parts`, technician-allowed); a two-step picker `/work-orders/[id]/parts/new` (machine **fitment quick-pick** → catalog search → qty) and a **"Parts used"** section on the job (list + remove + parts-cost total, read-only once Done). The machine page's existing per-job part counts now light up. **Verified in browser + DB across all 4 ACs** (issue linked to job, insufficient-stock refused with on-hand+bin, remove → reversing movement restores stock with both rows kept, Done job + machine history show parts & cost) — **invariant #1 (`SUM==on_hand`) asserted for every part after each op**. `next build` clean (21 routes). Hardened via the **adversarial review workflow** (invariant-correctness / concurrency-transaction / security-RBAC / UX-craft → verify) — invariant held under concurrency; **10 findings fixed:** global **SQLite `busy_timeout=5000`** so overlapping writers queue instead of erroring (+graceful `SQLITE_BUSY` catch), atomic in-tx job-status gate (closed the add/remove TOCTOU), exact **integer-cent** cost total with a **"N without a listed cost"** caveat (no more silently-understated total), stacked-SKU part row (name no longer crushed at 375px), 44px "Add part" target, and an Activity-log contrast bump (slate-400→500). **MMS-36 → Done**; MMS-53 (E2-S10) now fully satisfied. **Not committed.**
- **2026-07-23 (checkpoint commit):** Everything above through E3-S2/E3-S6 + the code-review fixes committed on branch **`feat/machines-stock-workorders`** (`f80f27a`, 53 files, +6805/−271). DB and `/uploads` stay untracked (gitignored). Not pushed — no remote yet (**D5 domain** open).
- **2026-07-23 (E3-S4 My work — BUILT + verified + reviewed):** A per-user **"My work"** view (`/my-work` + nav item) — the signed-in user's own open/in-progress jobs, overdue-first (reuses `searchWorkOrders({assigneeId})` + the shared `dueState`), with **one-tap Start/Done inline on the list** (open→in_progress flips the button to Done; done drops the row; `startWork`/`completeWork` now also revalidate `/my-work`). **Verified in browser** for both personas (Tomasz sees only his jobs; Maria hers), the full one-tap lifecycle updating in place, and mobile 375px (44px action fully in-viewport, no overflow). Focused independent review → 3 minor fixes (per-job button `aria-label`, future date in `Mono`, "Due today" clock + casing to match the dashboard/queue). `next build` clean (22 routes). **MMS-34 → Done.** Uncommitted (on top of `f80f27a`).
- **2026-07-24 (E3-S7 Photos + completion details — BUILT + verified + reviewed):** **Job photos + time-spent.** Multi-photo per work order via the polymorphic `photos` table (entityType `work_order`, filename a server `uuid.jpg` in `./uploads/jobs/`), client-compressed (≤1600px q0.85), served through an **auth-gated stream route** (`/work-orders/[id]/photos/[photoId]` GET) with `nosniff`; upload route (`.../photos` POST) enforces **≤10 MB, ≤10/job, magic-byte** validation; **full-screen lightbox** thumbnails stamped with uploader + time; delete restricted to uploader-or-admin on an open job. The image magic-byte check moved to `lib/uploads.ts` and is now **shared** by the parts + job routes. **Time-spent minutes** captured on completion (`completeWork`), shown on the done job, and **summed as machine labour** (`machineLaborMinutes`, shown "45m logged" on the machine page). **Verified in browser + DB:** upload→row+file, thumbnail stream 200 image/jpeg, lightbox stamp, delete (no orphan), and the guards — cross-job id **404** (IDOR), closed-job upload **409**, spoofed-mime HTML **415**; time-spent stored/displayed/summed; mobile 375px clean (3-col grid, lightbox covers viewport). `next build` clean (23 routes). Focused security+correctness+a11y review (path-traversal/auth/IDOR/magic-byte all clean) → **4 fixes:** partial-batch `router.refresh()` (don't hide already-saved photos on a mid-batch failure), lightbox **Escape-to-close + focus** management, `startWork`/`completeWork` now revalidate the **machine page** (labour total + completed-work), and a glyph-contrast bump. **MMS-37 → Done.** Uncommitted.
- **2026-07-24 (E3-S9 Reassign / reschedule — BUILT + verified + reviewed):** **Planner reassign / reschedule / reprioritize.** New admin-only server action **`updateWorkOrderPlan`** (`work:reassign`) changes a still-open job's assignee, due date, and priority, writing **one activity row per changed field** (old→new) into `wo_status_history` — a no-op logs nothing; the update + all log rows run in **one transaction** with graceful `SQLITE_BUSY` handling. An **inline editor** on the WO header (`plan-row.tsx`, admin + open-job only; `useActionState` + `ok`-flag `useEffect` collapse) swaps in over the assignee/due read-line; the Activity feed now renders each row's **note** (so reassignments — and completion/cancel notes — read in place). Closed jobs lock the plan (no Edit control, server refuses). **Verified in browser + DB across all ACs:** admin sees Edit / technician doesn't; change-all-three → header + 3 old→new activity rows + editor collapse + DB persisted; no-op adds nothing; unassign / clear-date format as "→ Unassigned" / "→ No date" and log only changed fields; Done job has no editor. Focused adversarial correctness/security/a11y review (authorization, change-detection, `ok`-collapse, revalidate set, note XSS, date round-trip, `toStatus`-reuse all came back clean) → **1 confirmed fix:** a since-deactivated current assignee stayed selectable in the editor (else a date-only edit silently unassigned + logged a false "reassigned"), with the matching server rule that only a *new* assignment must be active — **re-verified** (inactive assignee kept, priority-only save logged just "reprioritized"). `next build` clean (24 routes). **MMS-39 → Done.** Uncommitted (on top of `4d44571`).
- **2026-07-24 (E3-S5 Checklist — BUILT + verified + reviewed):** **Ordered checklist steps on a job.** Reused the existing `checklist_items` table; two new RBAC actions — **`work:manage-checklist`** (admin: add/remove steps) and **`work:check`** (any signed-in user: tick/untick). Server actions `addChecklistItem` (append at `max(position)+1`, ≤200 chars), `removeChecklistItem`, and a direct-call **`toggleChecklistItem(id, checked)`** that stamps `checkedBy`/`checkedAt` and returns `{ok}`. New client **`checklist.tsx`**: an **optimistic checkbox** (`useOptimistic` + `useTransition`) so a tick feels instant, a planner-only add-step form + per-row remove, and an inline "couldn't save" revert on a failed write. Rendered as section 3 (between the lifecycle action and Parts). **Finish-with-unticked-steps warning** (warn, never block) reuses `ConfirmSubmit` on the job page **and** the My Work one-tap Done (`uncheckedStepsFor` + a `confirmMessage` guard on `JobAction`), naming the exact open steps. **Verified in browser + DB across both ACs and personas:** tick saves immediately + survives refresh, stamped actor+time (Maria *and* Tomasz); admin adds/removes, technician only ticks (no add form / no remove); Done with 2 unticked → confirm naming them, **cancel keeps it in progress**, confirm completes; closed job → checkboxes disabled, no add/remove. Focused adversarial review (authorization, IDOR/cross-job on the id-only toggle, status-freeze, warn-not-block, optimistic revert, XSS, My Work parity all came back clean) → **1 low fix:** `toggleChecklistItem` now catches `SQLITE_BUSY` like its siblings and the client guards the await so a refused/contended tick **reverts the box instead of crashing** (there's no error boundary) — re-verified the happy path still saves. `next build` clean (24 routes). **MMS-35 → Done.** Uncommitted (on top of `4d44571`).
- **2026-07-24 (E3-S8 Breakdown → downtime close — BUILT + verified + reviewed):** **Finishing a breakdown job closes the machine's downtime.** New `resolveDowntimeForJob` action (`machine:set-status`, technician-allowed) does the *same* atomic close as the machine page's "Running" (one `UPDATE … WHERE machineId AND ended_at IS NULL`, `durationMs = nowMs − started_at`, `closedBy`) **and links the period to the job** (`downtime_periods.workOrderId`). New `openDowntimeFor` / `downtimeResolvedBy` queries. On a **done** job whose machine is still Down, an amber **prompt card** (`downtime-prompt.tsx`) asks "is it running again?" — confirm closes that period + links it; **declining is just not tapping** (machine stays Down, offer persists). Once closed, the job shows "**was down 2h 32m**" and the machine's downtime-history row shows "**Fixed by WO-N**" — both read from the period only (invariant #2), so the two views can never disagree. **Verified in browser + DB:** prompt on done+down, confirm → period closed with `closedBy`+`workOrderId`, machine flips to **Running**, job "was down" figure == machine-history figure, "Fixed by WO-4" links back. Focused adversarial review (invariant #2 integrity, wrong-machine/over-close, authorization, atomic close-vs-`markRunning`, revalidate set all came back clean) → **2 fixes:** (1, med) a job now resolves **at most one** period — the prompt is suppressed once the job has closed one, and the action no-ops if already linked or the job isn't `done`, so a *later* unrelated breakdown can't get re-attributed (was: prompt reappeared + duplicate "Fixed by"); (2) deterministic `ORDER BY` on `downtimeResolvedBy`. **Re-verified:** a second breakdown on the machine leaves the finished job showing only its own "was down", no prompt. `next build` clean (24 routes). **MMS-38 → Done.** **Epic E3 (Work Orders) COMPLETE** — all 9 stories done; **epic MMS-4 → Done**, and the earlier-pending **MMS-35 (E3-S5)** flipped to Done too (the Atlassian MCP had been timing out mid-session, cleared on retry). Uncommitted (on top of `4d44571`).
- **2026-07-24 (Vercel demo deploy — prep + live):** Made the app deployable on Vercel (serverless, read-only FS) alongside the VPS path: the libsql client is now env-driven (**Turso** remote DB via `DATABASE_URL`/`TURSO_DATABASE_URL` + token) and **lazy** (no DB connection at build — fixed a "collecting page data" build failure); photo storage abstracted (`lib/uploads` → **Vercel Blob** when `BLOB_READ_WRITE_TOKEN` is set, else local disk) and **gracefully disabled** when neither is present (upload UI hidden, routes 503). New `scripts/migrate-turso.mjs` + `scripts/seed-turso.mjs` (copies demo data; skips photos/transient rows; one demo password) + `VERCEL.md` runbook. Pushed to a **private GitHub repo** (`verticalDust/mms`, `main`, Vercel Git integration). Turso migrated + seeded (79 rows; invariant #1 verified on the remote DB). Live demo (non-photo) on `*.vercel.app`; logins `maria@northgate.example` / `tomasz@northgate.example` · `demo-mms-2026`.
- **2026-07-24 (E4 Preventive Maintenance — BUILT + verified + reviewed):** **Epic E4 COMPLETE (all 5 stories).** `lib/pm.ts` — the invariant-#3 core: `generateDuePmWorkOrders` creates one PM work order per active, unpaused, non-retired schedule due within 7 days, idempotent via the `(pmScheduleId, dueDate)` unique index **and** an "already has an open job" guard (so editing a due date can't spawn a second open job); `advanceScheduleAfterCompletion` floats next-due to completion+interval (day-granular). **E4-S1** create/edit/pause/resume/delete schedules (`pm/actions.ts`, `pm:manage`; machine-page section + `pm/new` + `[scheduleId]/edit` forms; interval ≥1 refused). **E4-S2** generation via a manual "Generate due jobs" button (`generatePmNow`) **and** a daily **Vercel cron** (`/api/cron/pm`, fail-closed on `CRON_SECRET`, `/api/cron/` exempted from the login gate). **E4-S3** floating next-due hooked into `completeWork` (PM jobs only). **E4-S4** overdue PM = ordinary work-order overdue (free) + a **PM badge** on the WO + overdue count on the register. **E4-S5** global **register** (`/pm` + nav) sortable by next-due, paused marked; **"No PM" filter** on the machines list. **Verified in browser + DB:** create (machine + register display, 3-step template), generate (WO with checklist/assignee/due/`PM-generated`), **idempotency** (re-generate → no dup), floating due (complete 24 Jul, 14-day → next 7 Aug; WO keeps its own due), bad interval refused, pause→resume, overdue register + row, no-PM filter excludes the scheduled machine. Focused adversarial review (idempotency/invariant-#3, floating-due math, auth, cron guard, delete FK, IDOR) → **3 fixes:** (1, med) generation now skips when the schedule already has an **open** job (edit-due-date no longer creates a 2nd) — **re-verified**; (2) cron **fail-closed** on `CRON_SECRET`; (3) `SQLITE_BUSY` handled in generation. `next build` clean (28 routes). **MMS-40…44 + epic MMS-5 → Done.**
- **2026-07-25 (E5 Public QR reporting — BUILT + verified + reviewed):** **Epic E5 COMPLETE (all 3 stories).** The `reports` table + `work_orders.source='report'`/`report_id` + `photos`/`report:triage` were already in the schema, so E5 was wiring, not migration. **E5-S1** the anonymous **report form** now fills the `/m/{code}` stub: machine header from the scanned QR, an **instant EN⇄BG toggle** (no reload, no data loss; the one Cyrillic surface — bilingual copy in `app/m/[code]/messages.ts`, no whole-app i18n framework), required description, optional name, camera-first optional photo, one big submit → **confirmation** page (§7.2); hardened per §1.5 — **honeypot** (silent, stores nothing), **per-machine burst (10/min) + daily (40) quota** (serverless-safe, DB-counted), **3 MB cap + magic-byte check**, **client downscale + server `stripJpegExif`** (drops EXIF/GPS APP1, keeps a valid JPEG — unit-tested), plus a friendly **dead-link** page for a retired/mangled code. **E5-S3** re-scan shows only **coarse status** (received / being worked / fixed / reviewed) via a per-device recent-report cookie — no names or notes; green-flip on fixed. **E5-S2** admin **triage queue** (`/reports` + desktop nav item + **untriaged badge**, slate; dashboard gauge now points here) — oldest-first cards with machine tap-through, description, reporter, photo thumb (auth-gated stream route), age; **Create work order** (prefilled, machine **locked to the report**; WO insert + report→handled flip in **one guarded transaction**) or **Dismiss** (reason required). **Verified in browser + DB:** form render + EN/BG toggle, submit → confirmation → status view (received→working→reviewed, both languages), honeypot stored nothing, triage create-WO (atomic bidirectional link) + dismiss (audited), badge count + green-flip, dead-link page; `stripJpegExif` unit-tested; `next build` clean (32 routes). Two-lens adversarial review (security/abuse + correctness/concurrency) → **6 fixes:** (1, med) report-photo route gated to **`report:triage`** not just any signed-in user (a technician could enumerate stranger photos); (2, med) **unguessable** photo storage key (random token — the public Blob URL isn't enumerable if the store base leaks); (3, med) **dismiss race** — the `UPDATE` now carries the `status='new'` guard + `rowsAffected` check (mirrors create-WO), so a dismiss racing a create can't strand a `dismissed` report on a live job; (4) `createWorkOrder` revalidates the **machine page**; (5) **burst cap 5→10** (both reviewers flagged genuine concurrent reporters being silently dropped on a hot machine); (6) a report on a since-**retired** machine shows a **"dismiss only"** state (was a dead-end Create link + a permanently-stuck badge). **Re-verified** the retired-state + guarded-dismiss in browser. Known trade-offs documented (not fixed): per-machine daily quota can be weaponized to suppress reports for 24h (needs a cross-machine/IP signal — v0.1); coarse-status is forgeable-but-public-only (no PII leak); a max-length description can split an emoji surrogate (cosmetic, editable). **MMS E5 stories + epic → Done.** Uncommitted; not yet pushed to the demo.
- **Remaining Week 3:** **CSV paste-import** (machines + parts); a repeatable **ledger invariant test** (`SUM==on_hand`, asserted manually so far).
- **Next:** **E3 + E4 + E5 done (epics closed).** MMS live as a Vercel demo (E4 pushed + PM demo-seeded). Remaining: Week-3 tail (CSV import + ledger test), then **E6 (dashboard + digest — the last epic)**. E5 is built + verified locally but **not yet committed/pushed** to the demo.

---

## 5. Design review log (2026-07-22)

Four-perspective review (domain, adversarial, delivery, pilot-user). Architecture unchallenged — monolith, SQLite, and the three invariants held. Five amendments folded into v1.1:

1. Public report form ships in Bulgarian too (D9) — the no-login form serves the least English-speaking persona; ~20 strings on existing i18n plumbing.
2. CSV paste-import + explicit pilot-onboarding checklist (D10) — pilot data entry was unbudgeted factory labor.
3. Week 4 rebalanced — E3-S2 (queue) pulled into week 3 so the L-story week carries less.
4. Anonymous form hardening beyond honeypot — photo cap, EXIF strip, per-machine quota.
5. Plumbing made explicit — hand-rolled sessions (D8), cron via `instrumentation.ts` (safe because idempotent), UTC-everywhere, uploads via route handler; D5 flagged as deploy-blocking.

**Open risk the review could not resolve:** the whole schedule assumes solo-with-AI velocity of ~7 stories/week. If week 1 comes in slow, cut Should-stories immediately rather than compressing week 6. Adoption (the daily-digest habit, per the backlog's E4 note) remains the pilot's real failure mode — watch it from pilot day 1.

---

## 6. Added v0 scope — Machine parts (fitment / BOM) · decided 2026-07-23 (D11)

**Status (2026-07-23): BUILT + verified + reviewed.** E2-S8/S9 and the E2-S10 reverse list shipped — `machine_parts` table (migration `0001_known_dagger.sql`), the attach flow (`/machines/[id]/parts/new`, two-step search → configure), the machine-page **Parts** section (attach/remove, admin), and the part-page **Fits machines** list. Verified in browser + DB: attach with qty/note, duplicate excluded from the picker + server-guarded, retired-machine attach refused, and **remove unlinks only — the part and its full stock ledger stay** (fitment decoupled from stock, confirmed in the DB). Adversarial review came back clean on correctness / security-RBAC / Next-RSC; 4 low/nit UX findings fixed (picker 50-cap signal, search query carried across the two steps, qty-0→null, qty in Mono). The job-logging quick-pick half of E2-S10 shipped with **E3-S6 (2026-07-23)** — a machine's fitment parts surface first ("Suggested for this machine") when logging parts on its work order. `next build` clean.

**Why.** Technicians and planners need to know which spare parts a given machine uses, so the right part is found fast during a repair and logging parts on a job is a couple of taps rather than a catalog hunt. Added to v0 at the user's request.

**Shape.** A machine↔part association — "this machine uses these spares," the industry's *bill of materials*. It is **informational and decoupled from the stock ledger** (invariant #1 is untouched): attaching or removing a part never creates a stock movement; it only records or removes the link. The payoff is E2-S10, where a machine's known parts surface first when a technician logs parts used on its work order (E3-S6). Fitment is not usage — the two are independent (a part can be attached to a machine yet never issued, and vice-versa).

**Schema** (`machine_parts`, added to §1.3 — a junction row):

| Column | Notes |
|---|---|
| `id` | PK |
| `machine_id` | FK → `machines` |
| `part_id` | FK → `parts` |
| `quantity` | nullable int — how many the machine uses (installed count) |
| `note` | nullable text — e.g. position, "front bearing" |
| `created_by`, `created_at` | actor + timestamp (DoD) |

`unique(machine_id, part_id)` enforces no duplicate attachment; indexed both directions so the machine→parts and part→machines views are both cheap. Deleting a link removes only the row — the part and its full stock history stay.

**Scheduling.** **S8 + S9 land in Week 3** (right after the E2 parts catalog exists — they need parts to attach). **S10** (the job-logging tie-in and the part-page reverse list) rides with **E3-S6 in Week 4**. Small: ~one table + a machine-page section + an attach/remove dialog + a reverse list. Est. ~0.5–1 day for S8/S9. Absorbs into the Week-3 budget; if the week is tight, S10 is the deferrable tail (S8/S9 stand alone).

**Permissions.** View: any signed-in user. Attach/remove: **Admin only** (`machine:manage`) — controls hidden for technicians *and* the server action 403s a direct call, per the §1.5 rule.

### E2-S8 — Attach a part to a machine (Admin) · Week 3 · S
*As a maintenance admin, I record which spare parts a machine uses, so the team finds the right part fast.*
- **Given** I'm on a machine page as Admin, **when** I open "Add part", **then** I can search the parts catalog by SKU or name and attach one, with an optional quantity and a short note (e.g. position).
- **Given** a part is already attached to the machine, **when** I attach it again, **then** it's rejected (no duplicate) — I edit the existing line instead.
- **Given** I attach a part, **then** it appears in the machine's Parts section at once, and the machine appears on that part's "Fits machines" list.
- **Given** I'm a Technician, **then** the attach/remove controls are not rendered *and* a direct call is refused (403).
- **Given** a machine is retired, **then** its parts list stays viewable (history) but adding new parts is disabled.

### E2-S9 — Machine "Parts" section + remove (view: all; remove: Admin) · Week 3 · S
*As a technician, I see the parts a machine uses on its page, so I know what to pull from the store.*
- **Given** a machine has attached parts, **when** I open its page, **then** a "Parts" section lists each part: mono SKU, name, on-hand vs min with a Low/OK status chip, bin, and (if set) qty + note; each row taps through to the part.
- **Given** a needed spare is at or under its minimum, **then** it carries the amber Low-stock chip, so a short spare is obvious at a glance.
- **Given** the machine has no attached parts, **then** the section shows a plain empty state ("No parts recorded for this machine yet.") + (Admin) an "Add part" action.
- **Given** I'm Admin, **when** I remove a part from the machine, **then** it's confirmed and only the link is deleted — the part and its stock history are untouched — and it drops off both the machine's Parts list and the part's "Fits machines" list.

### E2-S10 — Machine parts speed up logging parts on a job · Week 4 (with E3-S6) · S
*As a technician, the machine's known parts are offered first when I log parts on its job, so I record usage in fewer taps.*
- **Given** I'm logging parts on a work order (E3-S6) whose machine has attached parts, **then** those parts appear as quick-pick suggestions above the full-catalog search.
- **Given** I pick a suggestion, **then** the E3-S6 flow runs unchanged (on-hand decrements in the same transaction; ledger Issue linked to job+machine).
- **Given** the machine has no attached parts, **then** the part search behaves exactly as today (no regression).
- **Given** I'm on a part's page, **then** a "Fits machines" section lists the machines that use it, each tapping through to the machine.

**Open (lean calls, confirm at build).** (a) UI labels — "Parts" on the machine page, "Fits machines" on the part page (not "BOM" — jargon on the floor). (b) `quantity` means *installed count*, optional — not an order quantity. (c) Removing a part does **not** warn about open jobs that consumed it — fitment and usage are independent by design.
